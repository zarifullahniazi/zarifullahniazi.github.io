import React from "react";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";

type Props = {};

const Projects = (props: Props) => {
  return (
    <div>
      <h1>Projects</h1>
      <p className="">Most recent work</p>
    </div>
  );
};

export default Projects;
